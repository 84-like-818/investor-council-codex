# Livermore 项目说明

这个项目已经预置了一批 Jesse Livermore 相关来源。

你后续只需要做三件事：

1. 补充 `manifests/seeds_master.csv`
2. 把手工拿到的资料放进 `data/incoming_manual/` 或 `data/local_library/`
3. 反复跑 pipeline

输出报告统一在 `reports/`。
